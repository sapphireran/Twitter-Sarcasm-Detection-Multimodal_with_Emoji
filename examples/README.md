# Archive lab scripts

Run from the repository root with `PYTHONPATH=.`.

See [docs/examples.md](../docs/examples.md) for the table of scripts
and [docs/generated/](../docs/generated/) for checked-in snapshots.
