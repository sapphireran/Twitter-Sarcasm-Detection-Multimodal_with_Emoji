# Core experiment stack used by the 2023 UCPH CCS2 notebooks.
# Pin ranges stay loose so a modern laptop can still install them.
# The docs/examples in this repo run with numpy + the Python standard library.

numpy>=1.21
pandas>=1.3
nltk>=3.7
emoji>=2.0
gensim>=4.0
scikit-learn>=1.0
joblib>=1.1
matplotlib>=3.5
tensorflow>=2.8
keras-preprocessing>=1.1
