# Example script output

This page is filled in after the scripts in `examples/` are run on the committed `dataset/` files. Until then, run:

```bash
python3 examples/dataset_overview.py
python3 examples/cue_analysis.py
python3 examples/lexical_baseline.py
python3 examples/attention_walkthrough.py
```

The numbers belong in this file so later edits to the tokeniser or cue list can be diffed against a known snapshot.
